const { PrismaClient } = require('@prisma/client'); const prisma = new PrismaClient(); async function main() { const s = await prisma.supplier.findMany(); console.log('Suppliers:', s.map(x => x.businessId)); const l = await prisma.stockLocation.findMany(); console.log('Locations:', l.map(x => ({name: x.name, businessId: x.businessId}))); } main().finally(() => prisma.$disconnect());
